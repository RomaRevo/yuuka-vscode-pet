# Changelog

## Unreleased

## 1.5.1 - 行走动画修复

- 修复启用“减少动态效果”时经典制服与睡衣形象左右行走只移动位置、不播放腿部帧而形成滑步的问题。
- 显式点击地面行走时继续使用各形象自己的完整步态和帧时长；其他被动动画仍遵循减少动态效果设置。

## 1.5.0 - 睡衣外观与生产力可靠性

- 新增可选的淡蓝睡衣版优香形象，默认继续使用原有经典像素形象。
- 两套形象使用独立动画规格；睡衣版复用完整 v2 图集与标准动作时序，并以 090°/270°方向帧响应鼠标观察。
- 睡衣形象在运行时使用 `1.2×` 动画时间倍率，并同步延长一次性动作停留时间，使待机、行走和反馈节奏更舒缓。
- “场景”页扩展为“外观”页，可即时切换形象与本地背景。
- 现代化桌宠界面层级，统一圆角、间距、按钮、表单、列表与统计样式，并补齐窄侧栏适配和减少动态效果。
- 页签支持方向键、Home 与 End 键导航；任务和提醒保存失败时保留输入草稿并显示内联反馈。
- 扩展在 VS Code 启动完成后恢复计时与提醒；生产力状态操作改为串行处理并提供脱敏错误提示。
- 扩展可在本地与远程工作区中优先作为 UI 扩展运行，避免桌宠界面错误落到远程扩展宿主。
- 新增任务与提醒创建上限，保留旧版本超限数据，避免重载时静默截断。
- “今日任务”在自然日切换时移除已完成项、结转未完成项，并允许结转任务重新计入新一天。
- 修复暂停计时器恰好剩余 0 毫秒时被错误恢复为完整时长的问题。
- 专注页新增 `15 / 25 / 45 / 60` 分钟预设与 `1–180` 分钟自定义时长，并在计时运行或暂停期间锁定修改。

## 1.4.2 - 界面标题与情景台词优化

- 将扩展显示名调整为“桌宠：像素小优香”，活动栏标题显示为“桌宠：像素小优香”。
- 移除视图标题栏中与视图内按钮重复的两个英文命令入口。
- 工作锁定被打扰时改用更温和的提示，并将生产力、提醒、计时等情景台词扩充为多条原创候选。

## 1.4.1 - 关系化台词系统

- 将互动、摸头、轻戳和待机台词重构为独立的关系台词数据文件。
- 新增五档心情与四档好感的 `5×4` 选择规则，使手动设置的好感值具有稳定、可见的台词差异。
- 优先表现优香的认真核对、预算意识、嘴硬关心与关系升温后的亲近感。
- 保持任务完成、计时、提醒和错误反馈使用独立情境台词，避免关系状态影响生产力提示。
- 互动会先按当前心情与好感选择台词，再结算本次互动带来的关系变化，手动设定可即时准确生效。

## 1.4.0 - 生产力核心与个性化场景

### 专注计时

- 新增专注、短休息和长休息三阶段计时器，支持开始、暂停、继续、提前结束和重置。
- 使用绝对结束时间维护运行中的计时；隐藏或重开 Webview 后仍可恢复。
- 每完成设定数量的专注轮次后进入长休息；专注完成会庆祝并更新本地统计。
- 专注期间保持工作锁定，点击角色、地面或复位不会打断计时。

### 今日任务

- 新增工作区范围的今日任务列表，支持添加、完成、删除和选择当前专注任务。
- 完成反馈与庆祝动画接入角色状态；同一任务只计入一次完成统计。
- 不扫描仓库 TODO、源码、文件名或其他项目内容。

### 提醒与统计

- 支持一次性提醒，以及默认关闭的喝水提醒和整点提醒。
- 支持跨午夜静默时段；静默期间错过的提醒会在结束后处理，同一时间提醒会合并。
- 新增今日/本周专注次数、分钟、完成任务数和连续专注天数，并提供清除入口。
- 聚合统计只保存日期和数字，不保存任务文字、提醒文字或代码内容。

### 关系与场景

- 新增“关系”页：五档心情、`0–100` 好感滑块/数字框、关闭和重置入口。
- 关系值只影响低频台词、互动动作和待机动作，数据仅保存在本机 Webview 状态。
- 新增简洁办公室、千年风格和纯透明感三种本地场景；办公室背景与 `72×78` 角色帧和 `2×` 显示倍率匹配。
- 场景使用 VS Code 主题变量适配深色/浅色主题，运行时不加载网络资源。

### 隐私与工程质量

- 扩展运行时不访问网络，不读取源码、文档内容、文件名、工作区路径、终端文本或 Git 提交内容。
- 任务、提醒和计时器按工作区保存；全局统计只保存日期和数字。
- 构建脚本纳入生产力控制器和办公室场景资源，回归测试覆盖计时、统计、任务、提醒、关系、场景和工作锁定。

## 1.3.1

- Delay mouse observation until the pointer rests briefly, avoiding immediate turns
  during ordinary movement.
- Add a regression test for the dwell behavior.

## 1.3.0

- Add configurable save, typing, idle, focus, and VS Code Task reactions.
- Add head pats, pokes, horizontal drag placement, jump, think, and milestone actions.
- Add contextual dialogue, local mood and affinity, and low-frequency character events.
- Keep editor, terminal, Git, and network content outside the extension's runtime access.
